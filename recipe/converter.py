def G_convert(quantity, protein, carbs, fat, f):
    """
    """
    return protein, carbs, fat, f


def KG_convert(quantity, protein, carbs, fat, f):
    """
    """
    protein = (quantity * 10) * protein
    carbs = (quantity * 10) * carbs
    fat = (quantity * 10) * fat
    F = (quantity * 10) * f
    return protein, carbs, fat, F


def L_convert(quantity, protein, carbs, fat, f):
    """
    """
    protein = (quantity * 10) * protein
    carbs = (quantity * 10) * carbs
    fat = (quantity * 10) * fat
    F = (quantity * 10) * f
    return protein, carbs, fat, F


def ML_convert(quantity, protein, carbs, fat, f):
    """
    """
    return protein, carbs, fat, f
